install.packages("tidyverse", dep = TRUE) # Instalar package
library(tidyverse) # Usar package

install.packages("magic", dep = TRUE)
library(magic)

magic(6)

installed.packages() # Ver todos los packages que se han instalado antes
